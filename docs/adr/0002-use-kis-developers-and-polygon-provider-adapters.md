# Use KIS Developers and Polygon.io provider adapters

The MVP will start with real Korean and US market data integrations through interchangeable provider adapters, using KIS Developers and Polygon.io as the default providers. This avoids a sample-only MVP that cannot prove market-data behavior, while keeping the provider seam explicit so licensing, coverage, or pricing changes can be handled by replacing adapters rather than rewriting signal logic.

