# Use Recharts for MVP visualizations

The natural language stock search and research detail feature will use Recharts for MVP charting. Recharts is a better fit than TradingView Lightweight Charts, ECharts, or direct D3 for this phase because the product needs several professional dashboard visuals across price, indicator, contribution, risk, backtest, and portfolio panels, while preserving fast React implementation and testable chart-ready view models.

